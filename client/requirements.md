## Packages
recharts | Data visualization and time series charts
date-fns | Date formatting and manipulation
framer-motion | Smooth page transitions and UI animations
lucide-react | Icon set (already in base but ensuring availability)
clsx | Class name utility (often needed with tailwind-merge)
tailwind-merge | Class merging (standard with shadcn/ui components)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
